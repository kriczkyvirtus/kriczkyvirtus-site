# REPLIT PROMPT 4 — HUB CARD + END-TO-END QA

**Read `REPLIT-0-read-first.md` first. Do not start until Prompt 3 is verified.**
**Builds:** the Resources Hub card, then verifies the whole funnel as one journey.

No uploaded files. Everything here uses what's already deployed.

---

## 1 · RESOURCES HUB CARD

Add an entry to the Resources Hub following the existing card pattern exactly.

| Field | Value |
|---|---|
| `id` | `reinvest-harvest` |
| Title | Reinvest or Harvest |
| Subtitle | The Owner's Capital Allocation Scorecard |
| Accent | `#C8A24E` |
| Duration | 12 min |

⚠️ The hub builds each link as `` `/tools/${tool.id}` `` or similar. **This tool is not under `/tools/`** — it lives at `/reinvest-harvest`. Check how the hub constructs its hrefs and report what you find. If the card would produce the wrong URL, **stop and report** rather than moving the route or hard-coding an exception.

⚠️ Do not restyle the card component. Copy the existing structure, change only content props.

---

## 2 · END-TO-END RUN

Complete the funnel yourself as a real user would, on a **phone-sized viewport (390×844)**, and record what happens at each step.

```
1.  Open /reinvest-harvest
2.  Click the CTA
3.  Fill step 1 with a real inbox you can check
4.  Answer all 20 steps
5.  Read the teaser
6.  Land on /reinvest-harvest/next?t=...
7.  Click BOOK YOUR FIT CALL — confirm it scrolls
8.  Open the email
9.  Click the report link in the email
10. Read the report on the phone viewport
11. From report page 16, click the CTA
```

Record: **how long the 20 steps took**, and **anything that felt slow, confusing, or broken.**

---

## 3 · DATA INTEGRITY FOR THAT RUN

For the submission you just made, confirm every one of these:

- [ ] `reports/{token}.json` in Blob, all keys populated, `company` present
- [ ] Row in the "Reinvest or Harvest" tab — all 20 columns, none blank that shouldn't be
- [ ] Same row in Aggregated
- [ ] ActiveCampaign contact exists
- [ ] `Revenue Range` custom field matches what you selected, character for character
- [ ] `Website Lead` and `RH: Entered` both applied
- [ ] The correct tier tag(s) applied — two tags if sub-$1M
- [ ] Email received; check **both** the HTML and plain-text parts
- [ ] Email subject includes the company name
- [ ] Report link in the email opens the right report
- [ ] The PS link opens `/reinvest-harvest/next` and resolves the token

---

## 4 · FAILURE PATHS

Deliberately break things and confirm each degrades rather than collapses:

- [ ] `/reinvest-harvest/next?t=garbage` → booking page with roadmap, no result, no 404
- [ ] `/reinvest-harvest/next` with no token at all → same fallback
- [ ] `/r/garbage` → 404
- [ ] Submit with an invalid email → validation error, no partial write anywhere
- [ ] Temporarily break `RESEND_API_KEY` and submit → still returns 200, Blob and Sheets and AC all still written, failure logged
- [ ] Refresh the report page → renders identically, no re-write to Blob

---

## 5 · CROSS-SURFACE CONSISTENCY

The same person must see the same thing everywhere:

- [ ] Quadrant name identical on teaser, thank-you page, email, report
- [ ] Both pillar scores identical on all four
- [ ] Guess-vs-actual line consistent between teaser, email and report
- [ ] Roadmap card 01 says "You Are Here" on the thank-you page and report page 15, "Start Here" on the unresolved thank-you page
- [ ] Revenue-tier branch identical on the thank-you page and report page 16

---

## 6 · OUTSTANDING — confirm still true, do not build

These are known gaps, listed so you don't build them by accident:

- Both VSL blocks are placeholders — no video yet
- `Authority` and `Track Intent` fields still not created in ActiveCampaign
- The `RH: Entered` automation itself is built on the AC side, not here
- `/free-session-rh` was removed on purpose — do not recreate it

---

## REPORT BACK

```
HUB CARD
  hub file path:
  how it builds hrefs:
  card added?  resulting URL:
  correct?  Y/N   (if N — stopped, describe)

END-TO-END RUN
  time to complete 20 steps:
  anything slow / confusing / broken:
  step-by-step result of the 11 actions:

DATA INTEGRITY — result of each of the 11 checks

FAILURE PATHS — result of each of the 6 checks

CROSS-SURFACE — result of each of the 5 checks

STILL OUTSTANDING
  confirm the 4 known gaps are still gaps and you did not build them

NOT DONE / STOPPED ON

QUESTIONS
```
