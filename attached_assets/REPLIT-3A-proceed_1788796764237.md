# REPLIT — PROMPT 2 ACCEPTED, PROCEED TO PROMPT 3

Prompt 2 is accepted. The landing page and flow are correct, all four quadrant profiles resolve, the payload carries everything, and the push hold held.

**The short-phone compaction was the right call and it's approved.** I re-tested the same change independently: no font or line-height changes, all 20 steps fit 390×844, and option cards stay at 56px — well above the 44px tap-target minimum. The overflow you found was real; the earlier verification had been run at a more forgiving viewport.

---

## 1 · `ReinvestHarvestFlow.jsx` IS NOW YOURS

Your version differs from the reference copy, correctly — it has the API wiring and the compaction. **The repo file is now authoritative:**

```
c6f3ed7fe822ef2c42e3afa3bfffb4a0ca8037ded071a823974ba992448dd293
```

⛔ Do not re-copy, overwrite, or "restore" that file from any upload. If a future brief supplies a flow file, stop and report rather than overwriting. Changes to it will come as diffs against your version.

The same will apply to the thank-you page and report once you wire them.

---

## 2 · PASCALCASE AGAIN

| Copy this | To this |
|---|---|
| `reinvest-harvest-thankyou.jsx` | `artifacts/kriczky-virtus/src/tools/ReinvestHarvestThankYou.jsx` |
| `reinvest-harvest-report.jsx` | `artifacts/kriczky-virtus/src/tools/ReinvestHarvestReport.jsx` |

Rename only — content byte-identical, hashes must still match at the new path.

⚠️ **The report is 122KB / 1,382 lines.** Large writes have failed silently on this project before. Verify both the `sha256sum` and the line count before moving on.

---

## 3 · PROPS THE ROUTES MUST PASS

Both read from the Blob helper you built in Prompt 1.

**`/reinvest-harvest/next?t=<token>`** → `email`, `quadrantKey`, `revenueBand`, `ownerTier`, `resolved`

`resolved` defaults to `true` in the component. Pass `false` **only** when the token is missing or the Blob lookup returns null. Do not 404 — the page has a designed fallback that still works as a booking page.

**`/r/:token`** → `name` (first name), `email`, `scores`, `guess`, `revenueBand`, `ownerTier`, `token`, `shareUrl`

This one **does** 404 on a bad token. The difference is intentional.

---

## 4 · PUSH IS AUTHORISED AT THE END OF PROMPT 3 — after these pass

Once Prompt 3 is built and verified, commit and push. Prompt 4's live checks need a deployed site, so this is what unblocks them.

⛔ **Do not push until every one of these is true:**

- [ ] `/reinvest-harvest` renders the landing page
- [ ] `/reinvest-harvest/start` runs all 20 steps
- [ ] `/reinvest-harvest/next` renders in both resolved and unresolved states
- [ ] `/r/<valid>` renders 16 pages; `/r/garbage` 404s
- [ ] `/tools/reinvest-harvest` redirects to a page that exists
- [ ] `PREVIEW = false` in both new files
- [ ] Production build passes

If any fails, stop and report instead of pushing. This deploy replaces a currently-live tool, so a broken push is a live regression.

**After pushing, report the commit SHA and confirm the Vercel deploy succeeded.**

---

## 5 · EVERYTHING ELSE IN `REPLIT-3` STANDS

Particularly the "do not touch" list: the report's viewport `useEffect`, the `#rh-scheduler` anchor and CTA hrefs, the iClosed `iframe`, and the `8.5in` page widths.

---

## REPORT BACK

Use the block at the end of `REPLIT-3-thankyou-and-report.md`, plus:

```
FILENAMES
  thankyou copied to:        sha256 match?  Y/N
  report copied to:          sha256 match?  Y/N   line count:

FLOW FILE
  ReinvestHarvestFlow.jsx left untouched this prompt?  Y/N

PRE-PUSH CHECKLIST
  each of the 7 items above:  PASS / FAIL

PUSH
  pushed?  Y/N   commit SHA:
  Vercel deploy succeeded?  Y/N   deployed URL:
```
