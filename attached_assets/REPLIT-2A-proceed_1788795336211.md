# REPLIT — PROMPT 1 ACCEPTED, PROCEED TO PROMPT 2

Prompt 1 is accepted. The data layer looks right, the redirect is verified, `store-results` is untouched, and the ActiveCampaign branch was correctly preserved rather than rewritten.

Two things before you continue.

---

## ⛔ 1 · DO NOT COMMIT OR PUSH YET

You wrote:

> No commit or push was performed because this prompt did not request one.

**That was the right call, and it stays that way until Prompt 2 is finished.**

Here's why it matters. The old scorecard is **live right now** at `/tools/reinvest-harvest` and working for real visitors. Your redirect sends that path to `/reinvest-harvest`, which does not exist yet.

Pushing now would mean: old tool dead, new tool not built, anyone landing on either path gets a 404. A live regression with no replacement.

**Hold everything local until `/reinvest-harvest` exists.** Prompt 2 creates it. Push after that, so the redirect and its destination go live in the same deploy.

If you need a push mid-way for any reason, stop and report first.

---

## 2 · THE SEVEN UNVERIFIED CHECKS ARE DEFERRED, NOT DROPPED

You correctly marked these NOT RUN rather than claiming a pass:

```
3  POST returns 200 + token
4  reports/{token}.json exists with all keys
6  Reinvest or Harvest row with 20 columns
7  Aggregated row appears
8  email arrives, both parts, link resolves
9  AC contact with Revenue Range + tags
10 broken Resend key still returns 200
```

**None of these can run in the agent shell** — the API is serverless and the secrets aren't injected there. They need a deployed environment.

**They move to `REPLIT-4-hub-and-qa.md`**, which runs against the deployed site. Do not attempt them again in Prompt 2 or 3. Do not delete or work around the code they cover.

Static verification you did run — the SHA match, five unique 22-character tokens, the production build, the module import, the legacy redirect — is accepted as sufficient for this stage.

---

## 3 · CONTINUE WITH `REPLIT-2-landing-and-flow.md`

Two notes on it:

**§1.4 asks you to find the UTM capture layer.** You already answered this: `utmsFromReferer(req)` in `api/lead-capture.js`. Skip that discovery item, note it's answered, and move on.

**The landing file hash was corrected.** Use `45ac65510f0941dc57ff4c1f1fa23274a681a1f00bdc8775dbb7d8ab303b9a63` — the value in the current `REPLIT-0` and `REPLIT-2`. Ignore the earlier `c9448be6…`.

Everything else in Prompt 2 stands. Stop at its report-back.

---

## 4 · WHAT SHOULD BE TRUE WHEN PROMPT 2 IS DONE

- `/reinvest-harvest` renders the landing page
- `/reinvest-harvest/start` runs all 20 steps
- `/tools/reinvest-harvest` redirects to a page that now exists
- A completed run POSTs and logs its payload
- **Still nothing pushed**

At that point the redirect has somewhere to land, and the push becomes safe.

---

## REPORT BACK

Use the block at the end of `REPLIT-2-landing-and-flow.md`, plus:

```
PUSH HOLD
  confirmed nothing committed or pushed?  Y/N

REDIRECT
  /tools/reinvest-harvest now lands on a real page?  Y/N
```
