# Replit Agent Prompt — Fix Google Sheets Lead Data

## Six fixes needed in `lib/sheets.js` (and minor update to `api/lead-capture.js`):

---

### Fix 1: Aggregated tab not populating

The Aggregated tab is not receiving any data. Check for these issues:

1. The tab name might not match exactly. The tab is called `Aggregated` in Google Sheets. Make sure the code references it exactly as `'Aggregated'` (case-sensitive).
2. The Aggregated row has 10 columns (A:J) because it includes "Tools Completed" at the end. Make sure the range is `'Aggregated'!A:J` and the row array has exactly 10 values.
3. Add explicit error logging so we can see what's failing:

```javascript
try {
  const aggResult = await sheets.spreadsheets.values.append({
    spreadsheetId,
    range: "'Aggregated'!A:J",
    valueInputOption: "USER_ENTERED",
    insertDataOption: "INSERT_ROWS",
    requestBody: { values: [aggregatedRow] },
  });
  console.log(`[Sheets] Wrote to "Aggregated" tab for ${name} <${email}>`, JSON.stringify(aggResult.data));
} catch (err) {
  console.error(`[Sheets] Failed to write to "Aggregated":`, err.message, err.response?.data?.error?.message);
}
```

---

### Fix 2: Timestamp in EST

Change the timestamp from UTC to Eastern Time. Replace the timestamp formatting:

```javascript
// Convert to Eastern Time
const estTimestamp = new Date().toLocaleString("en-US", {
  timeZone: "America/New_York",
  year: "numeric",
  month: "2-digit",
  day: "2-digit",
  hour: "2-digit",
  minute: "2-digit",
  second: "2-digit",
  hour12: true,
});
```

Use `estTimestamp` instead of `timestamp || new Date().toISOString()` in the row array for the Timestamp column.

---

### Fix 3: Add "Revenue Band" column for Constraint Roadmap

The "Constraint Roadmap" tab now has a new column between "Tool" and "Scores/Summary" called "Revenue Band". The column order is now:

`Timestamp | Name | Email | Tool | Revenue Band | Scores/Summary | Answers | Percentage | Band | Link`

Update the row construction for the Constraint Roadmap tab to include the revenue value. In `lib/sheets.js`, when building the row:

```javascript
// Map revenue keys to readable labels
const REVENUE_LABELS = {
  "under_500k": "Under $500K",
  "500k_1m": "$500K – $1M",
  "1m_3m": "$1M – $3M",
  "3m_10m": "$3M – $10M",
};

const revenueBand = REVENUE_LABELS[summary?.revenue] || summary?.revenue || "";
```

For the Constraint Roadmap tab specifically, the row should be:
```javascript
const row = [
  estTimestamp,
  name,
  email,
  tabName,
  revenueBand,  // Revenue Band column (only meaningful for Constraint Roadmap)
  JSON.stringify(summary || {}),
  JSON.stringify(answers || {}),
  percentage,
  band,
  blobUrl || "",  // Link column (see Fix 6)
];
```

For the other tool tabs that don't have a "Revenue Band" column, keep the row as before (without the revenueBand field) so the columns align with their headers. The simplest way is to check the tool name:

```javascript
let row;
if (tool === "constraint-roadmap") {
  row = [estTimestamp, name, email, tabName, revenueBand, summaryStr, answersStr, percentage, band, blobUrl];
} else {
  row = [estTimestamp, name, email, tabName, summaryStr, answersStr, percentage, band, blobUrl];
}
```

And adjust the range accordingly:
```javascript
const range = tool === "constraint-roadmap" 
  ? `'${tabName}'!A:J`   // 10 columns with Revenue Band
  : `'${tabName}'!A:I`;  // 9 columns without Revenue Band
```

---

### Fix 4: Band column — keep as-is

The "Band" column categorizes leads by their overall score health. Keep the existing logic:
- Score >= 70: "Green Zone"
- Score >= 50: "Market"
- Score >= 30: "Discount"
- Score < 30: "Not Sellable"

No changes needed here.

---

### Fix 5: Remove color hex codes from scores

The `summary.categories` array contains objects with a `color` field (hex code like `#34D399`). When stringifying the summary and categories for the spreadsheet, strip out the color fields:

```javascript
// Clean color hex codes from scores before writing to sheet
function cleanScores(summary) {
  if (!summary) return {};
  const cleaned = { ...summary };
  if (cleaned.categories && Array.isArray(cleaned.categories)) {
    cleaned.categories = cleaned.categories.map(cat => {
      const { color, ...rest } = cat;
      return rest;
    });
  }
  return cleaned;
}
```

Use `cleanScores(summary)` when building the row:
```javascript
const cleanedSummary = cleanScores(summary);
const summaryStr = JSON.stringify(cleanedSummary);
```

---

### Fix 6: "Link" column — populate with Blob Storage URL

The last column (formerly "Full Scores", now "Link") should contain the URL to the generated HTML file in Blob Storage.

**For the Constraint Roadmap:** The roadmap URL is already generated by the `lead-capture.js` function. Pass it to the `appendLead` function:

In `api/lead-capture.js`, after the roadmap is generated and `roadmapUrl` is available, pass it to `appendLead`:

```javascript
appendLead({
  name,
  email,
  tool: tool || "constraint-roadmap",
  summary: summary || {},
  answers: req.body.answers || {},
  timestamp: timestamp || new Date().toISOString(),
  blobUrl: roadmapUrl || "",  // ADD THIS
}).catch(err => console.error("[Sheets] appendLead failed:", err));
```

**For other tools:** The `/api/store-results` function stores the HTML and returns a URL. If the client-side code receives this URL, it should include it in the lead-capture payload. However, the simpler approach is to have `store-results.js` also call `appendLead` with the blob URL after storing the HTML, or pass the URL back to `lead-capture`.

The simplest approach: update the `appendLead` function signature to accept `blobUrl`:

```javascript
async function appendLead({ name, email, tool, summary, answers, timestamp, blobUrl }) {
```

And include it in the row:
```javascript
// Last column is the Link
const link = blobUrl || "";
```

For the Constraint Roadmap, the `roadmapUrl` from blob storage goes here. For other tools, if `/api/store-results` is called separately, the URL won't be available at lead-capture time. In that case, leave it blank for now — we can backfill it later or have `store-results.js` update the sheet.

---

### Updated `appendLead` function (complete):

```javascript
async function appendLead({ name, email, tool, summary, answers, timestamp, blobUrl }) {
  const sheets = getSheets();
  const spreadsheetId = process.env.GOOGLE_SHEETS_SPREADSHEET_ID;
  const tabName = TOOL_TO_TAB[tool] || tool;

  // EST timestamp
  const estTimestamp = new Date().toLocaleString("en-US", {
    timeZone: "America/New_York",
    year: "numeric", month: "2-digit", day: "2-digit",
    hour: "2-digit", minute: "2-digit", second: "2-digit",
    hour12: true,
  });

  // Compute percentage and band
  let percentage = "";
  let band = "";
  if (summary?.totalScore != null) {
    percentage = summary.totalScore;
    if (percentage >= 70) band = "Green Zone";
    else if (percentage >= 50) band = "Market";
    else if (percentage >= 30) band = "Discount";
    else band = "Not Sellable";
  }

  // Clean color codes from scores
  const cleanedSummary = cleanScores(summary);
  const summaryStr = JSON.stringify(cleanedSummary);
  const answersStr = JSON.stringify(answers || {});
  const link = blobUrl || "";

  // Revenue band (for Constraint Roadmap)
  const REVENUE_LABELS = {
    "under_500k": "Under $500K",
    "500k_1m": "$500K – $1M",
    "1m_3m": "$1M – $3M",
    "3m_10m": "$3M – $10M",
  };
  const revenueBand = REVENUE_LABELS[summary?.revenue] || summary?.revenue || "";

  // Build rows based on tool type
  let toolRow, toolRange;
  if (tool === "constraint-roadmap") {
    // Constraint Roadmap has Revenue Band column: A:J (10 columns)
    toolRow = [estTimestamp, name, email, tabName, revenueBand, summaryStr, answersStr, percentage, band, link];
    toolRange = `'${tabName}'!A:J`;
  } else {
    // Other tools: A:I (9 columns)
    toolRow = [estTimestamp, name, email, tabName, summaryStr, answersStr, percentage, band, link];
    toolRange = `'${tabName}'!A:I`;
  }

  // Aggregated row always has 10 columns (includes Tools Completed)
  const aggregatedRow = [estTimestamp, name, email, tabName, summaryStr, answersStr, percentage, band, link, tabName];

  // Write to tool-specific tab
  try {
    await sheets.spreadsheets.values.append({
      spreadsheetId,
      range: toolRange,
      valueInputOption: "USER_ENTERED",
      insertDataOption: "INSERT_ROWS",
      requestBody: { values: [toolRow] },
    });
    console.log(`[Sheets] Wrote to "${tabName}" tab for ${name} <${email}>`);
  } catch (err) {
    console.error(`[Sheets] Failed to write to "${tabName}":`, err.message, err.response?.data?.error?.message || "");
  }

  // Write to Aggregated tab
  try {
    await sheets.spreadsheets.values.append({
      spreadsheetId,
      range: "'Aggregated'!A:J",
      valueInputOption: "USER_ENTERED",
      insertDataOption: "INSERT_ROWS",
      requestBody: { values: [aggregatedRow] },
    });
    console.log(`[Sheets] Wrote to "Aggregated" tab for ${name} <${email}>`);
  } catch (err) {
    console.error(`[Sheets] Failed to write to "Aggregated":`, err.message, err.response?.data?.error?.message || "");
  }
}
```

---

## Critical Instructions
- Replace the entire `appendLead` function in `lib/sheets.js` with the updated version above.
- Update `api/lead-capture.js` to pass `blobUrl: roadmapUrl` to `appendLead`.
- Do NOT change any frontend code.
- Do NOT change roadmap generation logic.
- Test by running through the Constraint Roadmap assessment — verify data appears in BOTH the "Constraint Roadmap" tab AND the "Aggregated" tab.
