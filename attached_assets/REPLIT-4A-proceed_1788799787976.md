# REPLIT — PROMPT 3 ACCEPTED, TWO ISSUES, THEN PROMPT 4

Prompt 3 is accepted and the site is live. Good work on the push — the GitHub API replay after the HTTPS credential failure was the right call, and verifying every remote tree hash against local before advancing the ref was exactly the right level of care.

**You found three real defects in the supplied report component.** All three were genuine, all three were missed by the reference build's own QA, and your fixes are correct:

1. `scores` was `useState` with hardcoded sample data instead of a prop — **every real report would have rendered someone else's scores**
2. no `PREVIEW` flag, so the debug toolbar would have shipped
3. `copied` state with no `share()` and no rendered control

The reference copies have been corrected to match. **`ReinvestHarvestThankYou.jsx` and `ReinvestHarvestReport.jsx` are now repo-owned**, like the flow file. Do not overwrite them from any future upload — stop and report instead.

---

## ⚠️ 1 · TWO VERCEL PROJECTS, ONE FAILING — investigate before Prompt 4

You reported:

> Production project `kriczkyvirtus-website`: succeeded.
> A second connected Vercel project, `kriczkyvirtus-site`, failed independently.

**This needs resolving, and it is not cosmetic.** Two projects deploying the same repo means every commit shows a red combined status, which destroys the signal — a real failure will look identical to this one and get ignored.

**Report, do not change anything yet:**

1. Which project serves `www.kriczkyvirtus.com`? Confirm from the domain config, not by inference.
2. Does `kriczkyvirtus-site` serve any domain at all, or is it orphaned?
3. Why did it fail? Get the error if the logs are reachable without installing anything.
4. When was `kriczkyvirtus-site` last green? If it has been failing since before this work, it is pre-existing and unrelated.
5. Are the two projects on different root directories or build settings?

⛔ **Do not delete, disconnect or reconfigure either project.** Report and stop on this item.

---

## 2 · LOCAL GIT CREDENTIALS ARE BROKEN

The HTTPS push failed on invalid credentials and you routed around it via the API. That worked, but it is not a repeatable path for routine work.

Report what you found: which remote, which credential type, and what specifically is missing. Do not attempt to fix it or request credentials.

---

## 3 · THEN RUN `REPLIT-4-hub-and-qa.md`

Everything in Prompt 4 can now run for real — the site is deployed and the services are reachable from it.

Two notes:

**§2b holds the seven checks deferred from Prompt 1.** They run against the deployed site now, using one real submission.

**The last one is destructive.** Breaking `RESEND_API_KEY` to prove the handler still returns 200 must be done last, on a throwaway submission, with the key restored immediately after. If you cannot restore it safely, skip that check and report rather than leaving the key broken.

⚠️ Prompt 4 §1 warns that the Resources Hub may build hrefs as `/tools/{id}`, which would produce the wrong URL for this tool. Check before adding the card. If it would be wrong, stop and report rather than moving the route or special-casing it.

---

## REPORT BACK

```
VERCEL
  which project serves www.kriczkyvirtus.com:
  does kriczkyvirtus-site serve any domain:
  why it failed:
  last green build on that project:
  differing root dir / build settings:
  (changed nothing?  Y/N)

GIT
  remote:
  credential type + what's missing:
  (attempted no fix?  Y/N)
```

Then the full Prompt 4 report block.
