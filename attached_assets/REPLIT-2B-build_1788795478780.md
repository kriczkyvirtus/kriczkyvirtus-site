# REPLIT — PROMPT 2 DISCOVERY ACCEPTED, BUILD AUTHORIZED

Discovery accepted. Both hashes match, the push hold is holding, and UTM is confirmed as already handled.

One correction, then build.

---

## 1 · USE PASCALCASE FILENAMES — do not keep the kebab-case names

You wrote:

> The supplied Reinvest or Harvest filenames are deliberately kebab-case and will be copied with their specified names beside the existing components.

**They weren't deliberate — they're just how they came out of the design tool.** The repo convention wins.

| Copy this | To this |
|---|---|
| `reinvest-harvest-landing.jsx` | `artifacts/kriczky-virtus/src/tools/ReinvestHarvestLanding.jsx` |
| `reinvest-harvest-flow.jsx` | `artifacts/kriczky-virtus/src/tools/ReinvestHarvestFlow.jsx` |

⚠️ **Rename only. Do not change a single byte of content.** The SHA-256 values verify the file contents, not the filename, so they must still match after the copy. Run `sha256sum` on the file at its new path and confirm.

The `lib/` files you created in Prompt 1 stay kebab-case — that matches the existing `lib/` convention. Components are PascalCase, lib is kebab. Leave both as they are.

---

## 2 · ROUTES ARE NOT UNDER `/tools/`

Every existing tool sits at `/tools/<name>`. **These two do not:**

```
/reinvest-harvest          →  ReinvestHarvestLanding
/reinvest-harvest/start    →  ReinvestHarvestFlow
```

That is intentional — this is a funnel with its own path, not another entry in the tools directory. Register them in `main.jsx` following the same pattern as the existing routes, just at these paths.

⚠️ `/tools/reinvest-harvest` keeps its redirect from Prompt 1. After this build it will land on the real landing page instead of a 404.

---

## 3 · EVERYTHING ELSE IN `REPLIT-2` STANDS

In particular:
- Do not alter the payload shape
- Do not remove the `console.log` of the payload
- Leave `AUTO_ADVANCE = true`
- On a 200, take the returned `token` and redirect to `/reinvest-harvest/next?t={token}` after the teaser

⛔ **Still do not commit or push.** `/reinvest-harvest/next` won't exist until Prompt 3, but that's a page the flow redirects *to* after submission — it doesn't break anything already live. The push still waits.

---

## REPORT BACK

Use the block at the end of `REPLIT-2-landing-and-flow.md`, plus:

```
FILENAMES
  landing copied to:            sha256 still matches?  Y/N
  flow copied to:               sha256 still matches?  Y/N

REDIRECT
  /tools/reinvest-harvest now lands on the real landing page?  Y/N

PUSH HOLD
  nothing committed or pushed?  Y/N
```
