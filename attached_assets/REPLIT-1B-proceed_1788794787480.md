# REPLIT — PROCEED

Your Prompt 1A report is accepted. The ActiveCampaign branch is correct as-is, the file integrity checks all pass, and your Sheets plan is right.

**One clarification before you build**, then you're clear to continue.

---

## ⚠️ THE EMAIL MOVES CALL SITE

You reported:

> `sendResultsEmail()` in `lib/email.js`, called by `api/store-results.js`.

That is the **old** architecture: the single-page scorecard stored an HTML snapshot, then `store-results` sent the email about it.

**The new funnel does not use `api/store-results.js` at all.** There is no HTML snapshot — the report is a React route reading JSON from Blob.

So:

- `renderResultsEmail()` is called from **`api/lead-capture.js`**, as step 6 of the handler in `REPLIT-1` §7.
- Do **not** wire it into `api/store-results.js`.
- Do **not** modify `api/store-results.js`. Other tools use it. Once the old scorecard is redirected away, its `reinvest-harvest` path simply stops being reached. Leave it — report that you left it.

If anything in `store-results` looks like it needs changing for this funnel, stop and report instead.

---

## WHAT TO DO NOW

Work through these in order and stop at the end:

1. **`REPLIT-1A` §2** — rebuild the "Reinvest or Harvest" header to 20 columns; implement the Aggregated mapping into its existing 15 columns, changing nothing there
2. **`REPLIT-1A` §3** — route the send through `renderResultsEmail()`, called from `lead-capture`, leaving the `TOOL_DETAILS` entry in place unused
3. **`REPLIT-1A` §4** — permanent redirect `/tools/reinvest-harvest` → `/reinvest-harvest`, old component file left on disk
4. **`REPLIT-1` §2** — token generation
5. **`REPLIT-1` §3** — Blob write and read helpers
6. **`REPLIT-1` §7** — the handler, in the stated order
7. **`REPLIT-1` §8** — run all ten verification checks

⚠️ `REPLIT-1` §4 is superseded by `REPLIT-1A` §2. `REPLIT-1` §6 is superseded by the verified table in the revised `REPLIT-1` §6. Do not follow the older versions of either.

⛔ Still no UI. Do not touch the landing page, flow, thank-you page or report — those are Prompts 2 and 3.

---

## ONE THING THAT WILL LOOK BROKEN AND ISN'T

Until Prompt 2 ships, **nothing on the site will exercise this code path.** `/tools/reinvest-harvest` will redirect to `/reinvest-harvest`, which does not exist yet and will 404.

That is expected. Report it, don't fix it. Prompt 2 creates that route.

To verify §7 and §8, POST directly to the API with `curl` rather than going through a page.

---

## REPORT BACK

Use the **REPORT BACK** block at the end of `REPLIT-1-data-layer.md`, plus these:

```
EMAIL CALL SITE
  renderResultsEmail() called from:
  api/store-results.js left unmodified?  Y/N

SHEETS
  "Reinvest or Harvest" header rebuilt to 20?  Y/N
  branch writes all 20 in order?  Y/N
  Aggregated — nothing added/renamed/reordered?  Y/N
  Aggregated mapping implemented?  Y/N
  H Band receives the score band?  Y/N

OLD ROUTE
  redirect implemented how:
  /tools/reinvest-harvest now 404s via /reinvest-harvest?  Y/N  (expected until Prompt 2)
  old component file left on disk?  Y/N
```
